export function SiteFooter() {
  return (
    <footer className="relative z-10 border-t border-primary/10 bg-background/70 px-4 py-6 text-center text-xs text-muted-foreground backdrop-blur-md">
      ミーム / Mimu — “meme” in Japanese. Upload, time your text to the frames, download.
    </footer>
  )
}
